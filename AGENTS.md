# 特菲奥及类似大宗商贸/品牌官网开发与部署规范指南 (Project Best Practices & Guidelines)

本文档沉淀了在构建跨国商贸、品牌出海及双向进出口贸易官网时的核心设计规范、代码原则与部署标准，系统会自动载入这些规则以指导后续开发与类似项目的构建。

---

## 1. 前端视觉与交互规范 (UI/UX Best Practices)

### 1.1 严禁在公开前端界面暴露内部管理控件 (Zero Internal Tools on Public UI)
- **规则**：所有公开展示的产品卡片、商超巡店卡片、资质证书模块中，**绝对不要直接放置内部开发或管理性质的按钮**（例如「替换实拍原图」、「本地上传」、「调试开关」等）。
- **原则**：公开页面必须 100% 呈现纯粹、严谨、高端的买家/访客视角，仅保留「B2B产品规格」、「索样/询价」、「查看高清原图」等商务功能。

### 1.2 首屏（Hero Banner）信息减负与结构化呈现 (Structured Hero Architecture)
- **拒绝大段文字墙**：严禁在首屏放置超过 100 字的密集文字块，避免给访客带来阅读疲劳。
- **双轮驱动/多板块结构化卡片**：
  - 将复杂的业务（如：进口代理 vs 自主品牌出口）拆解为 **左右对称或网格状的轻量结构化卡片**。
  - 每个卡片包含：清晰业务标题、亮色小标签（如「覆盖数千家商超KA」、「现货源头直供」）、1~2句凝练的核心价值主张。
- **信息分层**：
  - 统一社会信用代码、市监局登记信息、官方固话专线等合规资质，统一放置在 **Footer（页脚）** 及 **企业资质与实力独立模块** 中，不在首屏重复堆叠。

### 1.3 国际化多语言一致性 (Full Multilingual Parity)
- 新增或调整任何结构化组件时，必须同步维护所有支持语言的字典（中文 `zh`、英语 `en`、泰语 `th`、越南语 `vi`、日语 `ja`、韩语 `ko`、阿拉伯语 `ar`），确保多语言切换时排版不崩塌、文本无缺失。

---

## 2. 部署与域名配置标准作业流程 (SOP for Deployment & Custom Domains)

### 2.1 现代无服务器（Serverless / Edge）架构
- 无需购买传统 Linux 云服务器，优先采用 **Vercel / Cloudflare Pages** 进行静态构建与边缘 CDN 分发。
- 自带全球 DDoS 防护、自动申请与续期免费 HTTPS (SSL) 证书。

### 2.2 独立域名 DNS 解析标准
- **带 www 域名**：添加 `CNAME` 记录，主机记录填 `www`，记录值指向 CDN 别名（如 `cname.vercel-dns.com`）。
- **不带 www 根域名**：添加 `A` 记录，主机记录填 `@`，记录值指向平台 Anycast IP。

### 2.3 搜索引擎收录提交节点
- **顺序严格要求**：必须在**独立域名 DNS 解析完全生效、HTTPS 绿色证书就绪、线上能通过独立域名正常访问后**，再前往 Google Search Console 和百度搜索资源平台提交所有权验证与收录申请。
